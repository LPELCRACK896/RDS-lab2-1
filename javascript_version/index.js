function submit() {
    const code = document.getElementById('code').value;
    const data = document.getElementById('data').value;
    const parity = document.getElementById('parity').value;

    

    console.log(code, data, parity);
}